class Solution:
    def addStrings(self, num1: str, num2: str) -> str:
        # convert string to num
        # add them -> return added num as a string
        a = int(num1)
        b = int(num2)
        
        return str(a+b)
